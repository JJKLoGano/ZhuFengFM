1、关于 推荐列表点击进入专辑详情
        1）会自动播放一个曲目
            传递信息包含：专辑ID，曲目ID

            接口17  返回的数据是完整的专辑详情和当前的播放列表信息
                请求参数：专辑ID，不能获取曲目内容，无法自动播放

            接口20  返回的数据是制定专辑的播放列表（排序版本），用于再推荐点击自动播放，获取指定曲目，并且获取曲目自动播放顺序，用于后台播放

        2）从分类进入，直接使用17，不需要自动调用20

        3)UI ,TASK,ENTITY,CALLBACK,UI